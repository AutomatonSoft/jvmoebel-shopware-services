{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Report decimal string",
  "type": "string",
  "pattern": "^-?\\d+\\.\\d{4}$",
  "description": "Signed decimal with exactly 4 fraction digits. Used for money totals, AOV, and conversion rates."
}
